/**
 * Experiences marketplace paused.
 * Re-enable: uncomment api.ts, queries.ts, and activities.tsx sections,
 * then restore the full ExperiencesSection implementation from git history.
 */
interface ExperiencesSectionProps {
  onAddToCalendar: (title: string) => void;
}

export function ExperiencesSection(_props: ExperiencesSectionProps) {
  return null;
}
